spring:
  application:
    name: auth-service

  mail:
    host: smtp.gmail.com
    port: 587
    username: meexora.app@gmail.com
    password: bfcu otnj xbmd tfnr
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  datasource:
    url: jdbc:postgresql://localhost:5433/meexora_auth
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

  data:
    redis:
      port: 6379
      host: localhost


kafka:
  topic:
    user-registration-request: user-registration-request
    user-forgot-password-request: user-forgot-password-request

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

jwt:
  access-token-expiration-ms: 600000        # 10 хвилин
  refresh-token-expiration-ms: 604800000    # 7 днів
